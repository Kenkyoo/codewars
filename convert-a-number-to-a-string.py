def number_to_string(num):
    return str(num)

number_to_string(123)
number_to_string(-100)

#Explicacion del codigo
# La función `number_to_string` toma un número como argumento y devuelve su representación en forma de cadena.
# La función utiliza la función incorporada `str()` para convertir el número a una cadena.
# Luego, se llaman a la función `number_to_string` con dos ejemplos: el número